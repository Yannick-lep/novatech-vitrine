{% extends 'base.html.twig' %}

{% block title %}Service index{% endblock %}

{% block body %}
    <h1>Service index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>CreateAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for service in services %}
            <tr>
                
                <td>{{ service.name }}</td>
                <td>{{ service.description }}</td>
                <td>{{ service.createAt |date('Y-m-d H:i') : '' }}</td>
                <td>
                    <a href="{{ path('app_service_show', {'id': service.id}) }}">voir</a>
                    <a href="{{ path('app_service_edit', {'id': service.id}) }}">éditer</a>
                    {{ include('service/_delete_form.html.twig) }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_service_new') }}">Create new</a>
{% endblock %}
